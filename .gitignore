# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/

# Node / Vite
node_modules/
web/dist/

# NOTE: the built UI bundle (server/daq/static/) IS committed on purpose, so the
# app runs with just `pip install -e . && python -m daq` — no Node on the target.
# Rebuild it with `cd web && npm run build`. If you'd rather treat it as a build
# artifact, add `server/daq/static/` here and build before running.

# DAQ runtime
data/
*.dat
.daq-sw/

# Session/transport cruft
_to_delete/
*.tar.gz

# OS / editor
.DS_Store
.idea/
.vscode/
